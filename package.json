{
  "name": "app-library",
  "packageManager": "yarn@4.3.1",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "yarn workspaces foreach -A run build",
    "deploy": "yarn workspaces foreach -A run deploy"
  },
  "dependenciesMeta": {
    "serverless@4.1.12": {
      "unplugged": true
    }
  },
  "devDependencies": {
    "rimraf": "^6.0.1"
  }
}
