{
  "name": "app-library",
  "packageManager": "yarn@4.3.1",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build-all": "yarn workspaces foreach -A run build",
    "version-check-old": "yarn dedupe --strategy highest",
    "version-check": "",
    "build": "yarn run version-check && yarn run build-all"
  },
  "dependenciesMeta": {
    "serverless@4.1.12": {
      "unplugged": true
    }
  },
  "devDependencies": {
    "rimraf": "^6.0.1"
  }
}
