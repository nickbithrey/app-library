{
  "name": "@app-library/lambda",
  "packageManager": "yarn@4.2.2",
  "main": "dist/index.js",
  "devDependencies": {
    "@app-library/sls-plugin-registration": "workspace:^",
    "@types/aws-lambda": "^8.10.141",
    "@types/node": "^20.14.11",
    "jest": "^29.7.0",
    "rimraf": "^6.0.1",
    "serverless": "^4.1.12",
    "serverless-localstack": "^1.2.1",
    "serverless-plugin-monorepo": "^0.11.0",
    "typescript": "^5.5.3"
  },
  "dependencies": {
    "@app-library/core": "workspace:^",
    "@app-library/dynamodb": "workspace:^",
    "aws-lambda": "^1.0.7"
  },
  "scripts": {
    "build": "yarn run rimraf ./tsconfig.tsbuildinfo && yarn run tsc -b",
    "build:all": "yarn build",
    "local:start": "yarn build && yarn run serverless deploy --verbose"
  }
}
